# frozen_string_literal: true

RSpec.describe Moostar do
  it "has a version number" do
    expect(Moostar::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(false)
  end
end
